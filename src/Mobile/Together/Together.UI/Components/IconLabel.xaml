<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:d="http://xamarin.com/schemas/2014/forms/design"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       mc:Ignorable="d"
       x:Name="content"
       x:Class="Together.UI.Components.IconLabel">
    <Label FontFamily="{StaticResource MaterialFontFamily}"
           FontSize="{Binding Path=FontSize}">
        <Label.FormattedText>
            <FormattedString>
                <Span Text="{Binding Path=Type}" TextColor="{Binding Path=IconColor}"/>
                <Span Text="{Binding Path=Text}" TextColor="{Binding Path=TextColor}"/>
            </FormattedString>
        </Label.FormattedText>
    </Label>
</ContentView>