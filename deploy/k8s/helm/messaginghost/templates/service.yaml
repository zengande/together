apiVersion: v1
kind: Service
metadata:
  name: {{ include "messaginghost.fullname" . }}
  labels:
    {{- include "messaginghost.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "messaginghost.selectorLabels" . | nindent 4 }}
